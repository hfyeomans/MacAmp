# State
- Research + plan complete (see research.md, plan.md).
- Delivered coordinate-system analysis + corrected GEN sprite rects to unblock Milkdrop window investigation (see conversation output).
- Awaiting decision on implementing flip logic in `SkinSprites.swift` using the provided coordinates or a shared `flipY` helper.
