# Plan

1. Use the research findings to describe where Codex persists session data for `sessionId`-based workflows
2. Cite the documented file path (`~/.codex/sessions/<uuid>.jsonl`) and mention the JSONL storage pattern so the user knows how to inspect or back it up
